/**
 * Placeholder test so Jest finds the tests directory.
 * Add real unit/integration tests here or in other *.test.ts files.
 */
describe('setup', () => {
  it('should pass', () => {
    expect(1 + 1).toBe(2);
  });
});
